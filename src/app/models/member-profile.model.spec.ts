import { MemberProfileModel } from './member-profile.model';

describe('MemberProfileModel', () => {
  it('should create an instance', () => {
    expect(new MemberProfileModel()).toBeTruthy();
  });
});
