import { PeerMentorshipRegistration } from './peer-mentorship-registration';

describe('PeerMentorshipRegistration', () => {
  it('should create an instance', () => {
    expect(new PeerMentorshipRegistration()).toBeTruthy();
  });
});
