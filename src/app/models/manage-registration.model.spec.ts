import { ManageRegistration.Model } from './manage-registration.model';

describe('ManageRegistrationModel', () => {
  it('should create an instance', () => {
    expect(new ManageRegistrationModel()).toBeTruthy();
  });
});
