import { PeerMentorshipRegistrationModel } from './peer-mentorship-registration.model';

describe('PeerMentorshipRegistrationModel', () => {
  it('should create an instance', () => {
    expect(new PeerMentorshipRegistrationModel()).toBeTruthy();
  });
});
